---
name: file_manage
description: Workspace-scoped file operations (create, list, search/replace edit, edit-blocks). Use when the agent must read or write files inside a given directory; enforces paths stay within workspace.
metadata:
  short-description: Create, list, and edit files inside a workspace.
---